import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#9999",
      light: "blue",
    },
    secondary: {
      main: "#177",
      light: "green",
    },
    myTheme: {
      main: "#465",
      light: "gray",
    },
  },
});